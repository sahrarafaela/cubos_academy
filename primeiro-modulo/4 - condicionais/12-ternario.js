const saldo = 10;
// console.log(`Você tem ${saldo}`);
if (saldo > 1) {
    console.log(`Você tem ${saldo} reais`);
} else {
    console.log(`você tem ${saldo} real`);
}

//outra forma
if (saldo === 1) {
    console.log(`você tem ${saldo} real`);
} else {
    console.log(`Você tem ${saldo} reais`);
}